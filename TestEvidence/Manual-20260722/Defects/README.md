# Defect record

No unresolved software defect was confirmed during this manual execution.

Two scenarios were marked **BLOCKED**, not **PASS**:

- `MT-WH-08` required a configured genuine supplier-delivery receipt;
- `MT-WH-09` required qualifying QR scan records.

Their screenshots preserve the observed precondition/empty state. If either workflow later produces behaviour different from its requirement, a separate defect record should include the test ID, steps, expected result, actual result, severity, screenshot/log and resolution status.
