@{
    // This partial defines a URL prefix for views that are being rendered within the Admin area
    // but are using view files from the main app
    
    // Set the controller prefix to include the Admin area
    ViewData["ControllerPrefix"] = "/Admin";
} 